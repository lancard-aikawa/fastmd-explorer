# fastmd-explorer

ドキュメントファーストなリポジトリのための、軽量 Markdown ファイルエクスプローラー。
フォルダを指定するだけで再帰的に `.md` ファイルを一覧表示し、GitHub ライクなプレビューを提供します。

---

## インストール

**動作要件:** Node.js 18 以上 / pnpm

```bash
git clone https://github.com/yourname/fastmd-explorer.git
cd fastmd-explorer
pnpm install
```

> pnpm が未インストールの場合は `corepack enable` で有効化できます (Node.js 16.10 以上同梱)。
> 本リポジトリは `package.json` の `packageManager` で pnpm バージョンを pin しています。

---

## 起動

```bash
# ブラウザを自動で開いて起動
pnpm start

# 開くフォルダをあらかじめ指定
pnpm start /path/to/your/docs

# リモートの Markdown を指定 (URLモードで開く)
pnpm start https://example.com/README.md
```

起動後、ブラウザで `http://127.0.0.1:13847` が開きます。

起動引数は exe でも同じように使えます (`fastmd-explorer.exe C:\docs`)。
指定パスが存在しない場合は警告を出し、従来どおり前回開いていた状態で起動します。

---

## exe にビルドして配布

Node.js を持っていない端末にも配布できる単一 exe をビルドできます。

```bash
pnpm run build
```

実行後、`dist/fastmd-explorer.exe` (Windows x64、約 64MB) が生成されます。
exe をダブルクリックすると、Node.js が無くても利用できます。

> 別マシンで clone してビルドする際の前提・ハマりどころ・CI 例は [BUILD.md](BUILD.md) を参照してください。

### 単体ウィンドウ (window モード)

配布版の exe は **window モード**で動作します:

- **コンソール窓を出さない** — 起動時に黒い画面 (コマンドプロンプト) は表示されません
- **単体ウィンドウで開く** — Edge / Chrome を「アプリモード」(`--app`) で起動し、
  アドレスバーの無い独立ウィンドウとして開きます (Edge は Windows 10/11 標準)
- **閉じたら終了** — ウィンドウを閉じるとサーバ接続が切れ、数秒後にプロセスも自動終了します
- **専用ブラウザプロファイル** — 普段使いのブラウザとは独立したプロファイルで起動するため、
  既存のタブやセッションに影響しません
- **ウィンドウサイズ・位置の復元** — 前回終了時のウィンドウサイズ・位置を記憶し、次回起動時に復元します

> window モードは exe では既定で有効です。`WINDOW=0` で無効化 (従来のタブ起動)、
> 開発時の `pnpm start` でも `WINDOW=1` を付けると window モードで起動できます。

ビルドの仕組み:

1. **bundle** (`pnpm run build:bundle`) — esbuild が ESM ソースを 1 ファイルの CJS にバンドル
   (Top-level await の解消・`import.meta.url` のシム化はビルドスクリプトが対応)
2. **exe** (`pnpm run build:exe`) — `@yao-pkg/pkg` が Node ランタイムとバンドル + アセット
   (`public/`・mermaid・highlight.js テーマ等) を一つの exe にパッケージング
3. **patch** (`pnpm run build:patch`) — exe の PE サブシステムを GUI に書き換え、
   ダブルクリック時にコンソール窓が出ないようにする

> ビルド成果物の中間ファイル (`src/_bundle.cjs`、`src/.pkgrc.json`、`dist/`) は git 管理外です。
> window モード時のログは `%TEMP%\fastmd-explorer.log` に出力されます。

---

## 設定

プロジェクトルートに `mdexplorer.config.json` を作成して設定をカスタマイズできます。
サンプルをコピーして編集してください。

```bash
cp mdexplorer.config.json.sample mdexplorer.config.json
```

```jsonc
// mdexplorer.config.json
{
  "port": 13847,
  "network": "local",  // "local" = 自分のPCのみ | "lan" = LAN全体に公開
  "theme": "light"     // "light" | "dark"
}
```

> `mdexplorer.config.json` は `.gitignore` 済みです。環境ごとに異なる設定を安全に持てます。
> ポート・ネットワーク設定は環境変数でも上書きできます: `PORT=4000 NETWORK=lan pnpm start`

---

## 機能

### フォルダ選択と履歴

- ヘッダーのパス入力欄にフォルダパスを入力して **開く** または Enter
- 「履歴 ▾」ドロップダウンから過去に開いたフォルダをすぐに再選択
- 履歴は最大20件、`~/.mdexplorer/config.json` に自動保存
- 履歴やフォルダ参照から開き直した時は必ず再スキャンするため、アプリを閉じている間の増減も反映されます

### URLモード（リモート md の閲覧）

- ヘッダー左の「フォルダ / URL」トグルで切替。パス入力欄に `https://...` を入力しても自動で URL モードになります
- リモートの `.md` をサーバ経由で取得してプレビュー表示（読み取り専用。編集・タグ・被参照は使えません）
- 開いた URL は履歴に残り、サイドバーの一覧または「履歴 ▾」から再表示できます
- フォルダモードと URL モードは独立したワークスペースとして扱われ、切替えても各モードのタブが保持されます
- プレビュー内の `.md` リンクは同じウィンドウ内でたどれます（それ以外のリンクは別タブで開きます）

### ファイルツリー

- 選択フォルダを再帰的にスキャンし、`.md` ファイルを一覧表示
- `node_modules`、`.git`、隠しフォルダは自動スキップ
- ディレクトリはクリックで展開・折りたたみ
- 矢印キーで操作可能（`↑` `↓` でカーソル移動、`Enter` で開く、`→` `←` でフォルダの展開・折りたたみ）
- サイドバーの「画像」ボタンで画像ファイル（サムネイル付き）、「HTML」ボタンで `.html` / `.htm` の表示を切替（いずれも既定 OFF、選択は保存されます）
- `Ctrl+F` でファイル名・パスのインクリメンタル検索
- サイドバーの境界線をドラッグして幅を調整可能

### タブ表示

- ファイルを開くと VSCode 風のタブが追加される
- 複数ファイルを同時に開いておける
- 未保存の変更があるタブは `●` で表示
- タブの `×` ボタンまたは `Ctrl+W` で閉じる
- `Ctrl+Tab` / `Ctrl+Shift+Tab` でタブ切替
- タブごとに閲覧履歴を保持し、`←` / `→` ボタンまたは `Alt+←` / `Alt+→` で戻る・進む

### Markdown プレビュー

- GitHub ライクなスタイルでレンダリング
- コードブロックのシンタックスハイライト（highlight.js）
- Mermaid 図（フローチャート、シーケンス図など）のレンダリング
- プレビュー内の `.md` リンクをクリックするとツリーからファイルを開く
- Wiki リンク `[[ファイル名]]` / `[[ファイル名|表示名]]` に対応（クリックで該当ファイルを開く）
- プレビュー上部に更新日時・文字数・見出し数を表示
- `⤢` ボタンまたは `Ctrl+Shift+F` で本文のみのフル表示に切替
- `⎙` ボタンで印刷 / PDF 出力（UI 要素を除外して本文のみ出力）
- 改ページ位置などの印刷設定は `⚙` の「印刷」タブで調整できます

### HTML ファイルの表示

- サイドバーの「HTML」ボタンをオンにすると、ツリーに `.html` / `.htm` も表示されます
- HTML は Markdown 化せず、sandbox iframe で隔離描画します（ページ自身の CSS / JS がアプリの表示と衝突しません）
- Markdown 内のローカル html リンクからも同じウィンドウ内で開けます

### 画像

- サイドバーの「画像」ボタンでツリーに画像ファイルをサムネイル付きで表示（`.png` `.jpg` `.jpeg` `.gif` `.webp` `.svg` `.bmp`）
- サムネイルをクリックすると画像ビューワが開き、拡大・等倍・フィット表示を切替できます
- 編集モードのエディタに画像をドロップすると、同じフォルダに保存して `![]()` を自動挿入します

### アウトライン（TOC）

- 見出し（`h1`〜`h4`）からアウトライン（目次）をプレビュー右側に自動生成
- 項目クリックで該当見出しへスムーズスクロール
- `◀` / `▶` でパネル全体を折り畳み（開閉状態は記憶）
- 子見出しを持つ項目は `▼` / `▶` で階層単位に折り畳み・展開
- 境界をドラッグして幅を調整、ダブルクリックで自動幅に戻す

### リンク図

- ヘッダーの `🕸` ボタンでフォルダ内 `.md` の相互リンクを図示（cytoscape）
- `[リンク](file.md)` / `[[Wikiリンク]]` の参照関係をノード＝ファイル、矢印＝リンクで表示
- 図はマウスホイールで拡大縮小・ドラッグで移動、**ノードをクリックすると該当ファイルを開く**
- 「PNG保存」ボタンで図を画像として保存できます
- リンクを1本も持たない孤立ファイルは非表示（件数のみ表示）
- ボタンを押すまで解析・描画は一切行わないため、通常時の負荷はゼロ

### 全mdを1つのPDFに（結合出力）

- ヘッダーの `📑` ボタンでフォルダ内の全 `.md` をツリー順に1つの結合プレビューにまとめる
- `⎙ PDF出力` で印刷ダイアログを開き、まとめて PDF 化できる
- **ファイル間のセパレータ**: 各ファイルの前に扉（ファイルパス・更新日時・文字数）を挿入し改ページ
- **目次**: ファイル名を第1階層、各見出しを第2階層以降にネストした結合目次を自動生成
- **ファイル間リンク**: `.md` リンク / Wikiリンクを結合ドキュメント内のページ内リンク（該当ファイルの節）へ変換

### 編集

- `編集` ボタンまたは `Ctrl+E` でエディター表示に切替
- `Ctrl+S` で保存、保存後は自動的にプレビューに戻る
- `破棄` ボタンで編集内容を取り消し

### タグ・重要フラグ

- 各ファイルに自由なタグを付与（スペースはハイフンに自動変換）
- ☆ボタンまたは `Ctrl+I` で重要フラグを切替
- メモ欄に短いコメントを記入可能
- タグ・フラグ・メモは `~/.mdexplorer/tags/{フォルダのハッシュ}.json` に保存（対象フォルダは汚しません）
  - 旧バージョンがフォルダ内に作成した `.mdexplorer/tags.json` があれば、初回読み込み時に自動移行します

### テーマ

- `◐` ボタンでライト / ダーク テーマを切替（選択は保存され再起動後も維持）

### ステータスバー・設定パネル

- 画面下部のステータスバーに起動ポート・現在のフォルダ・PID・稼働時間を表示
- `⚙` ボタンで「設定とシステム情報」パネルを表示
  - **印刷**タブ: 見出し・水平線・front matter での改ページ、要素の分割抑止、リンク URL の併記
  - **システム情報**タブ: サーバ・プロセス・設定ファイルのパスを確認

---

## キーボードショートカット

| ショートカット | 動作 |
|---|---|
| `Ctrl+F` | ファイル検索にフォーカス（ファイル表示中はページ内検索） |
| `Ctrl+G` | 全文検索パネルの開閉 |
| `Ctrl+R` | ファイルツリーを再読み込み |
| `Ctrl+E` | 編集モードに入る |
| `Ctrl+S` | ファイルを保存（編集中のみ） |
| `Ctrl+I` | 重要フラグのオン/オフ |
| `Ctrl+Tab` | 次のタブへ |
| `Ctrl+Shift+Tab` | 前のタブへ |
| `Ctrl+W` | 現在のタブを閉じる |
| `Ctrl+Shift+F` | フル表示のトグル |
| `Alt+←` / `Alt+→` | ナビゲーション履歴を戻る / 進む |
| `↑` / `↓` | ツリーのカーソル移動（ツリーにフォーカス時） |
| `Enter` | ツリーのカーソル位置を開く / フォルダを展開・折りたたみ |
| `→` / `←` | ツリーのフォルダを展開 / 折りたたみ |
| `Escape` | 編集を終了 / 検索をクリア / パネルを閉じる |

---

## ファイル構成

```
fastmd-explorer/
├── src/
│   ├── cli.js               # エントリポイント
│   ├── server.js            # Express サーバー・API
│   ├── fileScanner.js       # 再帰スキャン・キャッシュ
│   ├── tagManager.js        # タグ管理
│   └── configManager.js     # 設定・履歴管理
├── public/
│   ├── index.html
│   ├── app.js
│   └── style.css
├── mdexplorer.config.json.sample   # 設定ファイルのテンプレート
└── package.json
```

---

## 注意事項

- **IPv6 無効:** サーバーは `127.0.0.1`（IPv4）にのみバインドします
- **ファイルシステム境界の警告:** WSL から Windows ネイティブFS（`/mnt/`）、ネットワークドライブ、異なるドライブへのアクセス時は警告を表示します
- **LAN 公開時のセキュリティ:** `"network": "lan"` はローカルネットワーク上の全端末からアクセス可能になります。信頼できるネットワークでのみ使用してください
