# fastmd-explorer TODO

新しい項目は**上の日付セクションに追加**していく（下ほど古い）。
状態は `[ ]` 未着手 / `[x]` 実装済み。実装したらチェックを付け、リリース済みならバージョンを併記する。

「なるべくシンプルなアプリ」を維持する。魅力的でもスコープを広げる項目（WYSIWYG編集・プラグイン機構・Git連携・複数ペイン・クラウド同期・タブのフル永続化）は見送る方針。

---

## 2026-07-16

### 実装済み

- [x] `pnpm start <フォルダ|URL>` の引数対応 — `cli.js` が argv を解釈し `initialRoot`/`initialUrl` をサーバへ。init は「サーバ生存状態優先」に並べ替え。不正引数は警告して従来起動にフォールバック
- [x] ツリーの矢印キー操作 — `↑/↓` 選択移動・`Enter` で開く・`←/→` で展開折畳。`#file-tree` を focus 可能にし、クリックでカーソル追従。サーバー変更なし

### 未着手（提案）

- [ ] README/MANUAL を 1.6.0 に追従 — HTML表示・画像トグル・起動引数・矢印キーが未記載
- [ ] 右クリックに「パスをコピー」「エクスプローラで開く」— 既存コンテキストメニューに2項目追加（コピーはクライアントのみ、エクスプローラは `explorer /select,<path>` の小 endpoint）
- [ ] （任意）前回開いていた1ファイルを起動時に復元 — `lastFile` を1件保存するだけ。タブ全復元はしない（※ `restoreWorkspaceTabs`/`loadTabState` が既存。要調査）

---

## 2026-07-03

### 実装済み

- [x] HTMLファイル (.html/.htm) の閲覧表示 — sandbox iframe で隔離描画、ツールバーの「HTML」トグルで表示切替、md 内のローカル html リンクからも開ける（v1.6.0）

---

## 2026-03-26

### 実装済み（難易度順に着手）

- [x] コードブロック コピーボタン — レンダリング後に DOM 操作するだけ、サーバー変更なし
- [x] ページ内検索（Ctrl+F） — プレビュー内テキストを走査、全文検索の仕組みを流用
- [x] テーブルソート — レンダリング済みテーブルへの DOM 操作
- [x] 最近開いたファイル — クライアント状態 + localStorage のみ
- [x] スクロール同期 — 編集モードでエディタ↔プレビューのスクロール連動（分割表示）
- [x] Markdown ツールバー — 編集モードに太字・見出し・リンク等のボタン
- [x] バックリンク表示 — 全文検索インフラを流用して参照元ファイルを一覧表示
- [x] 画像ドラッグ&ドロップ挿入 — エディタへの画像ドロップで `![]()` 自動挿入
